{% extends '::base.html.twig' %}

{% block body -%}
    <h1>wrocpopr list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Wroclaw</th>
                <th>Population</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_wrocpopr_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>
            <a href="{{ path('admin_wrocpopr_show', { 'id': entity.id }) }}">
                    {{ entity.wroclaw }}
            </a>
                 </td>
                <td>{{ entity.population }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_wrocpopr_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_wrocpopr_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_wrocpopr_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
