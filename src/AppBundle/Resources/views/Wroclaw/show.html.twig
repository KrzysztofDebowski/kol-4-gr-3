{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Wroclaw</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Wroclaw</th>
                <td>{{ entity.wroclaw }}</td>
            </tr>
            <tr>
                <th>Population</th>
                <td>{{ entity.population }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_wroclaw') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_wroclaw_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
